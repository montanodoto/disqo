import styled from 'styled-components';

const StyledNoteBuilder = styled.div`
  padding: 1rem;
  margin: 2rem 0;
  padding: 2rem;

  border-radius: 20px;
  background: #ebebeb;

  display: flex;
  flex-direction: column;

  h3 {
    margin: 0;
  }
`;

export default StyledNoteBuilder;
